<%# TODO: refactor into a component with multiple versions %>
<section id="hero" data-layout="hero-split">
    <br-container>
        <br-grid data-layout="50-50">
            <br-grid-item>
                <hgroup>
                    <h1><%= data.title %></h1>
                    <p><%= data.subtitle %></p>
                </hgroup>
            </br-grid-item>

            <picture>
                <source srcset="/images/<%= data.hero_image %>-600w.webp 600w,
                                /images/<%= data.hero_image %>-1200w.webp 1200w,
                                /images/<%= data.hero_image %>-1920w.webp 1920w"
                                type="image/webp">  
                <source srcset="/images/<%= data.hero_image %>-600w.jpg 600w,
                                /images/<%= data.hero_image %>-1200w.jpg 1200w,
                                /images/<%= data.hero_image %>-1920w.jpg 1920w" 
                                type="image/jpeg">
                <img src="images/<%= data.hero_image %>-1200w.jpg" width="1200" height="806" loading="lazy">
            </picture>

        </br-grid>
    </br-container>
</section>

<style>

    section[data-layout="hero-split"] {
        padding-block: var(--size-fluid-6);
        // background-color: var(--gradient-3);
        // background-image: var(--hero-gradient);
    }

    section[data-layout="hero-split"] > br-container > br-grid[data-layout="50-50"] {
        --grid-alignment: center;
        text-align: left;
    }

</style>