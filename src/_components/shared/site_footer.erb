<footer data-theme="dark" class="">
<br-container>

  <br-grid>
  
    <div class="flow">
      <p class="text-bold">
        Bridgetown Rise
      </p>
      <p>
        A Bridgetown starter template.
      </p>
    </div>

    <nav>
      <p class="text-bold">Quick Links</p>
      <ul role="list">
        <% site.data.nav.each do |item| %>
          <li>
            <%= link_to item.title, item.url %>
          </li>
        <% end %>
      </ul>
    </nav>

    <div class="flow">
      <p class="text-bold">Social Links</p>
      <nav id="social" class="site-foot__inner" aria-label="Social links">
        
        <%# TODO: refactor into a flex container %>
        <% site.data.social_svg.each do |item| %>
          <a href="<%= item.url %>" class="flex">
            <span class="sr-only"><%= item.title %></span>
            <sl-icon-button name="<%= item.icon %>" label="<%= item.title %>"></sl-icon-button>
          </a>
        <% end %>

      </nav>
    </div>
    
  </br-grid>
</br-container>

  <footer>
      <p>&copy; <%= site.time.strftime("%Y") %> Developed by 
      <a href="https://grittypublishing.com">Gritty Publishing</a>
    </p>
  </footer>

</footer>